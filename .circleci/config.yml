version: 2.1

jobs:
  test:
    docker:
      - image: alpine
    steps:
      - checkout
      - run:
          name: Testing
          command: pwd; ls -la

workflows:
  test-workflow:
    jobs:
      - test
